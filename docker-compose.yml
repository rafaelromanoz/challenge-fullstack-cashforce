version: '3'

services:
  mysql:
    image: mysql
    container_name: cashforce
    environment:
      environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=true
      - MYSQL_DATABASE=cashforce_v3
    restart: always
    ports:
      - "3306:3306"
    expose:
      - "3306"